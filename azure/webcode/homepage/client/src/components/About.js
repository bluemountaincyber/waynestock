import React from "react";

export default function About() {
    return (
        <div>
            <h1>About</h1>
            <p>This is a simple app that demonstrates how to deploy a React app to Azure using GitHub Actions.</p>
        </div>
    );
}